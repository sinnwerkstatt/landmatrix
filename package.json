{
  "scripts": {
    "start": "concurrently npm:backend npm:frontend npm:caddy",
    "backend:init": "poetry install && poetry run doit initial_setup",
    "backend": "poetry run ./manage.py runserver",
    "frontend:init": "cd newfront && npm install && npm run build",
    "frontend": "cd newfront && npm run preview",
    "test:init": "playwright install chromium",
    "test": "./test.sh",
    "caddy": "caddy run"
  },
  "devDependencies": {
    "@playwright/test": "^1.22.1",
    "@types/node": "^17.0.43",
    "concurrently": "^7.4.0"
  }
}
